<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flyway.admin.mapper.AdminNotificationMapper">

    <insert id="insertNotification" parameterType="com.flyway.admin.dto.AdminNotificationDto">
        INSERT INTO  admin_notification (
            notification_id,
            notification_type,
            title,
            message,
            related_resource_type,
            related_resource_id,
            priority,
            is_read,
            created_at
        ) VALUES (
            #{notificationId},
            #{notificationType},
            #{title},
            #{message},
            #{relatedResourceType},
            #{relatedResourceId},
            #{priority},
            'N',
            NOW()
        )
    </insert>

</mapper>