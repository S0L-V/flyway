<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flyway.pricing.mapper.PriceHistoryMapper">

    <insert id="insertEventHistory"
            parameterType="com.flyway.pricing.model.PriceHistoryInsert">
        INSERT INTO price_history (
            price_history_id,
            flight_id,
            cabin_class_code,
            current_price,
            old_price,
            update_type,
            trigger_ref_id,
            policy_version,
            calc_context_json
        ) VALUES (
                     #{priceHistoryId},
                     #{flightId},
                     #{cabinClassCode},
                     #{currentPrice},
                     #{oldPrice},
                     #{updateType},
                     #{triggerRefId},
                     #{policyVersion},
                     #{calcContextJson}
                 )
    </insert>

</mapper>