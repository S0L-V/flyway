# ===== AWS RDS 연결 정보 (예제) =====
# 사용 방법:
# 1. 이 파일을 복사: application-dev.properties
# 2. db.password에 팀장에게 받은 비밀번호 입력

db.driver=org.mariadb.jdbc.Driver
db.url=jdbc:mariadb://flyway
db.username=admin
db.password=팀장에게_받은_비밀번호_입력

# HikariCP 설정
db.hikari.maximum-pool-size=10
db.hikari.minimum-idle=5
db.hikari.connection-timeout=30000
db.hikari.idle-timeout=600000
db.hikari.max-lifetime=1800000
